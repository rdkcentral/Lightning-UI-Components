import { ArgTypes, Meta, Title, Description } from '@storybook/blocks';
import * as ButtonStories from './Button.stories.js';

<Meta of={ButtonStories} />
<Title />
<Description of={ButtonStories} />

## Example of using ArgTypes from Storybook Blocks API

<ArgTypes of={ButtonStories} />

## Source

https://github.com/rdkcentral/Lightning-UI-Components/blob/develop/packages/@lightningjs/ui-components/src/components/Button/Button.js

## Usage

Used to convey an interactable portion of the interface, a button can present text, an icon, a checkbox, or both text and an icon or checkbox.It has support in all the modes.

```js
import { Button } from '@lightningjs/ui-components';

class Example extends lng.Component {
  static _template() {
    return {
      Button: {
        type: Button,
        title: 'Button'
      }
    };
  }
}
```

### Sizing

By default, `Button` widths dynamically adjust to account for `title` and `icon` widths and `padding`, with a minimum width defined by `minWidth`.

If you want a specific `Button` width no matter what the size of content, set the `fixed` property to `true`. The `minWidth` here will still be honored.

If you want a `Button` that is fully dynamic, set `minWidth` to `1`.

If you'd like to have a fixed button with a width smaller than the `minWidth`, you'll need to set the `minWidth` to a value less than or equal to the provided width.

## API

### Properties

| name    | type            | readonly | default   | description                                                 |
| ------- | --------------- | -------- | --------- | ----------------------------------------------------------- |
| fixed   | boolean         | false    | false     | when true, `w` overrides dynamically calclulated width      |
| justify | string          | false    | 'center'  | alignment of the button's content                           |
| prefix  | object \| array | -        | undefined | Lightning components to be placed to the left of the title  |
| suffix  | object \| array | -        | undefined | Lightning components to be placed to the right of the title |
| title   | string          | false    | undefined | button text                                                 |

### Style Properties

| name            | type                          | description                                                                            |
| --------------- | ----------------------------- | -------------------------------------------------------------------------------------- |
| contentColor    | string                        | color to be applied to the prefix and suffix icons                                     |
| justify         | 'center' \| 'left' \| 'right' | alignment of the button's content                                                      |
| minWidth        | number                        | used as a fallback width if no width is passed into the component and `fixed` is false |
| paddingX        | number                        | space between the button horizontal edge and the content                               |
| paddingXNoTitle | number                        | space between the button horizontal edge and the content if no title is provided       |
| textStyle       | string \| object              | text style to apply to the title                                                       |
| contentSpacing  | number                        | padding between title and prefix                                                       |
