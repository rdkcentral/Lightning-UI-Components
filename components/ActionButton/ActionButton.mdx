import { Preview, Story } from '@storybook/addon-docs/blocks';

# ActionButton

General purpose button component

## Usage

The basic component just requires a title:

```js
import { ActionButton } from '@lightning/ui';

class Basic extends lng.Component {
  static _template() {
    return {
      ActionButton: {
        type: ActionButton,
        title: 'My Button'
      }
    };
  }
};
```

<Preview>
  <Story id="actionbutton--basic" />
</Preview>

Before the button recieves its title, it should be in a loading state.
<Preview>
  <Story id="actionbutton--loading" />
</Preview>


When focused, the background and text colors are inverted.

<Preview>
  <Story id="actionbutton--focused" />
</Preview>

When and icon is present, the text should shift and all of the content should remain centered in the button.
<Preview>
  <Story id="actionbutton--icon" />
</Preview>

## API

### Properties

name|type|readonly|description
--|--|--|--
-|-|-|-
announce|string|true|announcement text for screen readers
h|number|false|height of the button
title|string|false|button text
unfocusIconUrl|string|false|URI for icon that may live next to the title
w|number|false|width of the button
