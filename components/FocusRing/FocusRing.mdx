import { Canvas, Story } from '@storybook/addon-docs/blocks';
import { Theme } from '../../.storybook/utils';
import FocusRing from '.';

# FocusRing

Fancy ring to focus on Items

## Usage

Add a focus ring to tile or other items to provide a nice highlight
and shadow effect

```js
import { FocusRing } from '@lightning/ui';

FocusRing: {
  type: FocusRing,
  w: this.w,
  h: this.h,
  size: 12
}
```

<Canvas>
  <Story id="focusring--basic" />
</Canvas>

### Properties

name|type|readonly|description
--|--|--|--
-|-|-|-
size|number|false|set width of the ring (even values only)

### Styles

Styled components can be adjusted by composing using [withStyles](?path=/docs/mixins-withstyles--basic). Below is the default `styles` object

<Theme theme={FocusRing.styles} />