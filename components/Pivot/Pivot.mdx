import { Preview, Story } from '@storybook/addon-docs/blocks';

# Pivot Button

Button component used specifically for pivoting to new experiences.

## Usage

The basic component just requires a title:

```js
import { Pivot } from '@lightning/ui';

class Basic extends lng.Component {
  static _template() {
    return {
      Pivot: {
        type: Pivot,
        title: 'Pivot'
      }
    };
  }
};
```
Before the button receives its title, it should be in a loading state.
<Preview>
  <Story id="pivot--loading" />
</Preview>


<Preview>
  <Story id="pivot--unfocused" />
</Preview>


When focused, the background and text colors are inverted.
<Preview>
  <Story id="pivot--focused" />
</Preview>

If a really long title is provided, a Pivot will dynmically size itself to provice 16px of padding
to the left and right of the text inside the Pivot.
<Preview>
  <Story id="pivot--long-title" />
</Preview>

## API

### Properties

name|type|readonly|description
--|--|--|--
announce|string|true|announcement text for screen readers
title|string|false|button text
