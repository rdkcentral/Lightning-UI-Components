---
platform: linux
inputs:
  - name: source-repo
outputs:
  - name: source-repo-ready
    path: source-repo
image_resource:
  type: docker-image
  source:
    repository: node
    tag: 16.0.0
run:
  path: ./ci/tasks/install.sh
  dir: ./source-repo
