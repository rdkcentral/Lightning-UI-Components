---
platform: linux
inputs:
  - name: source-repo
outputs:
  - name: source-repo/dist
caches:
  - path: source-repo/node_modules
image_resource:
  type: docker-image
  source:
    repository: node
run:
  path: bash
  args: [ ./ci/tasks/publish.sh ]
  dir: ./source-repo
