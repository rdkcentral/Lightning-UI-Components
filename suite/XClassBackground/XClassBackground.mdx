import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import XClassBackground from '.';

# XClassBackground

Short component description

## Usage

Explain how `XClassBackground` works with examples:

```js
import { XClassBackground } from '@lightning/ui';

class Example extends lng.Component {
  static _template() {
    return {
      XClassBackground: {
        type: XClassBackground,
        w: 1920,
        h: 1080
      }
    };
  }
};
```

<Canvas>
  <Story id="suite-xclassbackground--basic" />
</Canvas>
