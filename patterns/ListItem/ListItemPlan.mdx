import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import { ListItemPlan } from '.';

# ListItemPlan

A [`ListItem`](?path=/docs/listitem--basic) with more lines of text content and [`Radio`](?path=/docs/toggle--basic) functionality.

## Usage

`ListItemPlan` can display a `title`, `subtitle`, `content`, and `disclaimer`. The radio itself sits on the right side.

```js
import { ListItemPlan } from '@lightning/ui';

class Example extends lng.Component {
  static _template() {
    return {
      ListItemPlan: {
        type: ListItemPlan,
        title: 'ListItemPlan Title',
        subtitile: 'ListItemPlan subtitle',
        content: 'ListItemPlan content',
        disclaimer: 'ListItemPlan disclaimer'
      }
    };
  }
}
```

<Canvas>
  <Story id="patterns-listitemplan--basic" />
</Canvas>

`ListItemPlan` will always toggle on <kbd>Enter</kbd>. This can not be overriden.

To get the current checked state, use the `isChecked()` method.

```js
if (ListItemPlan.isChecked()) {
  // do something
}
```

## API

### Properties

| name           | type                                         | readonly | description                                                                      |
| -------------- | -------------------------------------------- | -------- | -------------------------------------------------------------------------------- |
| title          | string                                       | false    | the 1st line, main item info goes here, maxLines=2                                           |
| subtitle       | string                                       | false    | the 2nd line, item metadata info goes here, maxLines=0(default), this line won't be shown if the property was set to falsy value |
| content        | string                                       | false    | the 3rd line, the details of item, maxLines=6, this line won't be shown if the property was set to falsy value |
| disclaimer     | string                                       | false    | the 4th line, the disclaimer of item, maxLines=2, this line won't be shown if the property was set to falsy value |
| checked        | boolean                                      | false    | sets initial `checked` value of Radio. Use `toggle()` to change value after init |


### Methods

#### isChecked():boolean

returns the checked value (boolean) for the Radio

#### toggle():boolean

Toggles the `checked` state and returns the new state.

### Styles

Styled components can be adjusted by composing using [withStyles](?path=/docs/mixins-withstyles--basic). Below is the default `styles` object

<Theme theme={ListItemPlan.styles} />
