export default theme => ({
  imageResize: 96,
  itemSpacing: theme.spacing(0),
  padding: {
    y: theme.spacing(1)
  },
  paddingTop: theme.spacing(0),
  imageRadius: theme.spacing(2),
  getFocusScale: theme.getFocusScale
});
