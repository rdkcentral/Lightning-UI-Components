import { Canvas, Story } from '@storybook/addon-docs';
import CardAbout from './CardAbout';

# CardAbout

Component that shows title, aligned icons and text in the middle, and description below.

## Source

https://github.comcast.com/Lightning/lightning-ui/packages/@lightning/ui-core/src/components/CardAbout/CardAbout.js

## Usage

`CardAbout` can display ratings, icons, and titles and descriptions for different types of content.
`CardAbout` extends [Card](?path=/docs/patterns-card--base)

```js
import { CardAbout } from '@lightning/ui';

class Basic extends lng.Component {
  static _template() {
    return {
      CardAbout: {
        type: CardAbout,
        title: 'CardAbout',
        description:
          'CardAbout allows you a lot of customization with titles, icons, and descriptions'
      }
    };
  }
}
```

<Canvas>
  <Story id="patterns-CardAbout--about-cards" />
</Canvas>

## API

### Properties

| name      | type             | required | default | description                                                                |
| --------- | ---------------- | -------- | ------- | -------------------------------------------------------------------------- |
| iconLeft  | string \| object | false    | -       | icon left source to be passed to InlineContent component                   |
| iconRight | string \| object | false    | -       | icon right source to be passed to InlineContent component                  |
| textLeft  | string \| object | false    | -       | text on the left side of the card to be passed to InlineContent component  |
| textRight | string \| object | false    | -       | text on the right side of the card to be passed to InlineContent component |

### Style Properties

| name                      | type   | description                                            |
| ------------------------- | ------ | ------------------------------------------------------ |
| contentSpacing            | number | spacing between left and right side content            |
| descriptionTextProperties | object | textProperties of the description                      |
| paddingFirstLine          | number | padding between the top of the card and the first line |
| paddingVertical           | number | padding vertically between lines                       |
| inlineContentStyle        | object | properties of the middle InlineContent container       |
| titleTextProperties       | object | text properties of the title                           |
