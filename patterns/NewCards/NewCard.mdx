import { Canvas, Story } from '@storybook/addon-docs';
import NewCard from '.';

# NewCard

NewCards are very configurable. New Cards is the foundational basis for all other card variations.

## Usage

NewCards work by providing many of the relevant properties from TextBox, Surface, and Tile.

```js
import { NewCard } from '@lightning/ui';

class Basic extends lng.Component {
  static _template() {
    return {
      NewCard: {
        type: NewCard,
        title: 'NewCard'
      }
    };
  }
}
```

<Canvas>
  <Story id="patterns-newcard--base" />
</Canvas>

## API

### Properties

| name  | type   | required | default | description             |
| ----- | ------ | -------- | ------- | ----------------------- |
| title | string | false    | -       | headline of the content |

### Style Properties

| name                     | type   | description                                                                  |
| ------------------------ | ------ | ---------------------------------------------------------------------------- |
| backgroundColorDisabled  | string | color of the card when disabled                                              |
| backgroundColorFocused   | string | color of the card when not disabled and when focused                         |
| backgroundColorUnfocused | string | color of the card when unfocused and not disabled                            |
| paddingHorizontal        | number | padding on the x-axis between the right and left of the card and its content |
| paddingVertical          | number | padding on the y-axis between the top and bottom of the card and its content |
| radius                   | number | corner radius of card                                                        |
| textColorDisabled        | string | color of the text when disabled                                              |
| titleTextProperties      | object | style of the title text                                                      |
