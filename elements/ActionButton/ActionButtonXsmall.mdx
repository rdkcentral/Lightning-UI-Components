import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import { ActionButtonXsmall } from '.';

# ActionButtonXsmall

Xsmall [ActionButton](?path=/docs/actionbutton--base).

## Usage

The basic component just requires a title:

```js
import { ActionButtonXsmall } from '@lightning/ui';
// or
// import { ActionButtonXsmall } from '@lightning/ui/components/ActionButton';

class Basic extends lng.Component {
  static _template() {
    return {
      ActionButton: {
        type: ActionButtonXsmall,
        title: 'Action Button',
        backgroundType: 'stroke'
      }
    };
  }
};
```

An `icon` can also be included. The expected icon size is 32px by 32px. See [Icon](?path=/docs/icon--basic) for options.

<Canvas>
  <Story id="elements-actionbuttonxsmall--base" />
</Canvas>


Before the Action Button receives its title, it should be in a loading state.
<Canvas>
  <Story id="elements-actionbuttonxsmall--loading" />
</Canvas>

## API

### Properties

name|type|required|default|description
--|--|--|--|--
announce|string|true|label or title|announcement text for screen readers
backgroundType|'stroke'\|'fill'\|'float'\|'ghost'|false|'float'|color of `Button` container.
fixed|boolean|false|false|when true, `w` overrides `minWidth`
h|number|false|48|height
icon|string|false|-|URI for icon or inline svg that may live next to the title. See <a href="?path=/docs/icon--basic">Icon</a>
minWidth|number|false|152|minimum width of the button
padding|number|false|16|left and right padding value
radius|number|false|8|border radius
title|string|false|-|button text
w|number|false|-|width

### Methods

#### onEnter(thisArg): void

Callback to be executed on <kbd>Enter</kbd> key down event

### Styles

Styled components can be adjusted by composing using [withStyles](?path=/docs/mixins-withstyles--basic). Below is the default `styles` object

<Theme theme={ActionButtonXsmall.styles} />
