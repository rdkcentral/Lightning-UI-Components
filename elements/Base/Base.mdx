import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import Base from '.';

# Base

This component is used to provide a solid starting point for component development. It includes our default mixins and some class methods.

## Usage

Explain how `Base` works with examples:

```js
import Base from '../../elements/Base';

class MyNewComponent extends Base {
  static _template() {
    return {
      // ...
    };
  }
};
```

## API

### Properties

name|type|required|default|description
--|--|--|--|--
withVibrant|boolean|false|false|if true, the Base component will be wrapped in the [withVibrant](?path=/docs/mixins-withVibrant--basic)

### Methods

#### methodName(args): returnValue

Method description

### Styles

Styled components can be adjusted by composing using [withStyles](?path=/docs/mixins-withstyles--basic). Below is the default `styles` object

<Theme theme={Base.styles} />
