import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import Radio from '.';

# Radio

A radio has 2 states: "on" or "off". When used in a group, it should let a user select only one of the choices at a time. This can be managed with the withSelections mixin

## Usage

Simple component that just needs a type.

```js
import { Radio } from '@lightning/ui';

class Example extends lng.Component {
  static _template() {
    return {
      Radio: {
        type: Radio
      }
    };
  }
}
```

<Canvas>
  <Story id="elements-radio--basic" />
</Canvas>

## API

### Properties

| name       | type    | required | default | description                          |
| ---------- | ------- | -------- | ------- | ------------------------------------ |
| checked    | boolean | false    | false   | toggle state                         |
| isInactive | boolean | false    | false   | state where toggle cannot be changed |
| w          | number  | true     | 32      | width of Radio                       |
| h          | number  | true     | 32      | height of Radio                      |

### Style Properties

| name                    | type   | description                                               |
| ----------------------- | ------ | --------------------------------------------------------- |
| strokeColor             | string | color of the Radio border                                 |
| knobColor               | string | color of the Radio knob                                   |
| backgroundColor         | string | background color of the Radio                             |
| backgroundColorOff      | string | color of Radio background when not checked                |
| backgroundColorInactive | string | color of Radio background when Radio is inactive/disabled |
| strokeColorInactive     | string | color of Radio border when Radio is inactive/disabled     |
| radius                  | number | border radius                                             |
| strokeWidth             | number | border width                                              |
| knobHeight              | number | height of knob                                            |
| knobWidth               | number | width of knob                                             |

### Methods

#### toggle(): void

Toggles `checked` state and updates UI accordingly

### Styles

Styled components can be adjusted by composing using [withStyles](?path=/docs/mixins-withstyles--basic). Below is the default `styles` object

<Theme theme={Radio.styles} />
