import { Canvas, Story } from '@storybook/addon-docs';

# Icon

Renders icon paths and inline svgs

## Usage

`Icon` requires three properties, `icon`, `w` and `h`

```js
import { Icon } from '@lightning/ui';

class Example extends lng.Component {
  static _template() {
    return {
      Icon: {
        type: Icon,
        w: 50,
        h: 50,
        icon: 'path/to/icon.png'
      }
    };
  }
}
```

A valid `icon` value can be any path to any [common image type](https://developer.mozilla.org/en-US/docs/Web/Media/Formats/Image_types) or an inline SVG.

With an image path:

<Canvas>
  <Story id="elements-icon--basic" />
</Canvas>

With an inline svg:

<Canvas>
  <Story id="elements-icon--inline-svg" />
</Canvas>

## API

### Properties

| name | type   | required | default | description                     |
| ---- | ------ | -------- | ------- | ------------------------------- |
| icon | string | true     | -       | path to image or inline SVG XML |
| w    | number | true     | 0       | texture width                   |
| h    | number | true     | 0       | texture height                  |

### Style Properties

| name  | type   | description                                   |
| ----- | ------ | --------------------------------------------- |
| color | string | icon color (can only be applied on png icons) |
