import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import MetadataSmall from '.';

# MetadataSmall

Content to be displayed inside Tile using the metadataType in [Tile](?path=/docs/tile--basic). Displays title, info, progress bar, and logo.

## Usage

Pass Metadata in as metadataType for a Tile component

```js
import { MetadataSmall, Tile } from '@lightning/ui';

class Example extends lng.Component {
  static _template() {
    return {
      TileWithMetadata: {
        type: Tile,
        src: 'asset/path/pets.png',
        w: 410,
        h: 230,
        metadataLocation: 'inset',
        metadataType: MetadataSmall,
        metadata: {
          title: 'Movie Title',
          data: 'A description of the movie',
          logo: 'url/path/logo.png'
        }
      }
    };
  }
}
```

Include previews:

<Canvas>
  <Story id="patterns-withmetadata--inset-metadata" />
</Canvas>

The `id` for the preview comes from the url path generated for the story.

## API

### Properties

| name     | type                                                     | required | default | description                                                          |
| -------- | -------------------------------------------------------- | -------- | ------- | -------------------------------------------------------------------- |
| title    | string                                                   | false    | -       | the first line of text                                               |
| data     | [InlineContent](?path=/docs/layout-inlinecontent--basic) | false    | -       | relevant content data at the bottom                                  |
| progress | number                                                   | false    | 0       | percentage of the current progress from 0 to 1. Hidden if value is 0 |
| logo     | string                                                   | false    | -       | URI of a logo to display at bottom of component                      |
| logoW     | number                                                   | false    | -       | width the logo should take up                      |
| logoH     | number                                                   | false    | -       | height the logo should take up                     |

### Styles

Styled components can be adjusted by composing using [withStyles](?path=/docs/mixins-withstyles--basic). Below is the default `styles` object

<Theme theme={MetadataSmall.styles} />
