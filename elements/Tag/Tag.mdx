import { Canvas, Story } from '@storybook/addon-docs';
import { Theme } from '../../.storybook/utils';
import Tag from '.';

# Tag

General purpose component to help label content.

## Usage

Tags simply require a background color, a title, and a title color. The Tag will automatically adjust based on the rendered width of its text.

```js
import { Tag } from '@lightning/ui';

class Example extends lng.Component {
  static _template() {
    return {
      Tag: {
        type: Tag,
        title: 'Tag',
        style: {
          backgroundColor: '#f6a50a',
          titleColor: '#ffffff'
        }
      }
    };
  }
}
```

<Canvas>
  <Story id="elements-tag--basic" />
</Canvas>

## API

### Properties

| name  | type   | required | default | description            |
| ----- | ------ | -------- | ------- | ---------------------- |
| title | string | true     | -       | text to display in tag |

### Style Properties

| name              | type   | description                                       |
| ----------------- | ------ | ------------------------------------------------- |
| backgroundColor   | string | color of the tag's background                     |
| titleColor        | string | color of text                                     |
| textStyle         | string | text properties of tag                            |
| paddingX          | number | padding between left and right of text and tag    |
| paddingY          | number | padding between top and bottom of text and tag    |
| radiusTopLeft     | number | determines the shape of the top left's corner     |
| radiusTopRight    | number | determines the shape of the top right's corner    |
| radiusBottomRight | number | determines the shape of the bottom right's corner |
| radiusBottomLeft  | number | determines the shape of the bottom left's corner  |
